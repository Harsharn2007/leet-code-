class Solution:
    def reverseDegree(self, s):
        total = 0

        for i in range(len(s)):
            reverse_pos = 26 - (ord(s[i]) - ord('a'))
            total += reverse_pos * (i + 1)

        return total
